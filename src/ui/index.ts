// ui/ owns the drawers, tray cards, filters and controls. The volatile layer.
// Expect this to be rewritten several times, which is the whole reason core/ and state/ are walled off from it.

export { createUploadZone } from './upload'
export type { UploadZone } from './upload'
