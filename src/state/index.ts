// state/ owns where every piece is, which cluster it belongs to, and which drawer group it sits in.
// Pure data plus commands that mutate it. No pixi.js, no DOM.
//
// It may import types from core/. It may not import render/, ui/ or lab/.
//
// Empty until phase 7. Kept here so the boundary rules have something to point at.

export {}
